import React, { Component } from "react";

export default class Title extends Component {
  render() {
    return (
      <header>
        <h1>Seize the day!</h1>
        <button>Log out</button>
      </header>
    );
  }
}
